const PRIMARY = '#0095F6';
const LIGHT = '#B2DFFC';
const DARK = '#00479A';
const BLACK = '#202020';
const PRIMARY_LIGHT = '#E9E9E9';

// TODO:
// - colocar TODAS as cores aqui p/ exportar

const colors = {
  primary: PRIMARY,
  light: LIGHT,
  dark: DARK,
  blackUI: BLACK,
  primary_light: PRIMARY_LIGHT,
};

const theme = {
  colors,
  font: {
    fontFamily: 'Poppins, sans-serif',
  },
};

export default theme;
