const PRIMARY = '#0095F6';
const LIGHT = '#B2DFFC';
const DARK = '#00479A';
const BLACK = '#202020';

// TODO:
// - colocar TODAS as cores aqui p/ exportar

const colors = {
  primary: PRIMARY,
  light: LIGHT,
  dark: DARK,
  black: BLACK,
};

const theme = {
  colors,
  font: {
    fontFamily: 'Poppins, sans-serif',
  },
};

export default theme;
