import { mapImag } from '@assets';

import { MapImagStyle } from './PhotoMap.styles';

const PhotoMap = () => {
  return <MapImagStyle src={mapImag} alt="Mapa com as filiais" />;
};

export default PhotoMap;
